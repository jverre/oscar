# Convex Functions

This directory contains the backend Convex functions for Oscar Chat.

## Setup

1. Run `npx convex dev` to start the Convex development server
2. Follow the prompts to create a new Convex project
3. The dev server will generate TypeScript types in `_generated/`