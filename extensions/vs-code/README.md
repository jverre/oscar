# Oscar VS Code Extension

A VS Code extension to sync AI chat conversations with Oscar.

## Development

1. **Open extension directory as workspace:**
   ```bash
   code /path/to/oscar/extensions/vs-code
   ```

2. **Install dependencies:**
   ```bash
   npm install
   ```

3. **Debug:**
   - Press `F5` to compile and launch
   - Check Debug Console for logs